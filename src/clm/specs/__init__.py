"""
Classes responsible for creating spec files.

The modules in this package serve to build or update spec files from the course data
on disk.

They should neither be needed to build a `Course` instance from a spec nor to build
the output files for the course.
"""
